[package]
name = "fs-windows-test-harness"
version = "3.10.0"
edition = "2021"
license = "MIT"
description = "Runner for fs-windows-test-harness: lib + run-matrix bin"
publish = false

[lib]
name = "fs_windows_test_harness"
path = "src/lib.rs"

[[bin]]
name = "run-matrix"
path = "src/bin/run-matrix.rs"

[dependencies]
libtest-mimic = "0.7"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
anyhow = "1"

[profile.release]
opt-level = 3
codegen-units = 1
lto = "thin"
